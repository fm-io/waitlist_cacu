import React from 'react'

function Header({ onOpen }) {
  return (
    <header>
        <h1 className='cacu-circle'>Cacu</h1>
        {/* <button className="btn" onClick={onOpen}>Join the waitlist</button> */}
    </header>

  )
}

export default Header